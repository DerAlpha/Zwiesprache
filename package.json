{
  "name": "zwiesprache",
  "version": "0.1.0",
  "private": true,
  "description": "Serverlose, sichere 1:1-Chat-Web-App über WebRTC mit manuellem Code-Austausch",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/hashes.mjs dist",
    "build:single": "vite build --mode single && node scripts/single.mjs && node scripts/hashes.mjs dist-single",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "check": "npm run typecheck && npm run test && npm run build && npm run build:single"
  },
  "dependencies": {
    "jsqr": "1.4.0",
    "preact": "10.29.8",
    "uqr": "0.1.3"
  },
  "devDependencies": {
    "@babel/core": "7.29.7",
    "@playwright/test": "1.56.1",
    "@preact/preset-vite": "2.10.6",
    "@types/node": "22.20.4",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vite-plugin-singlefile": "2.3.3",
    "vitest": "5.0.1"
  }
}
